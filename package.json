{
  "name": "ralph",
  "version": "0.1.0",
  "description": "Ralph Orchestrator - Autonomous coding agent orchestration",
  "type": "module",
  "scripts": {
    "start": "bun ralph.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit",
    "typecheck:watch": "bunx tsc --noEmit --watch",
    "build": "echo 'No build step needed - Bun runs TypeScript directly'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
